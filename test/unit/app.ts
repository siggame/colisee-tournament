import * as chai from "chai";
import { App } from "../../src/app";

export default function () {
  describe("| App", () => {

    it("should be constructable", () => {
      const app = new App();
    })
  })
}
